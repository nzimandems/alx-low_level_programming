#include "main.h"
#include <stdio.h>

/**
 * print_rev - print a string in reverse
 * @s: pointer value
 *
 * Return: string value
 */
void print_rev(char *s)
{
	int count = 0;

	while (s[count])

	count++;

	while (count--)
	_putchar(s[count]);
	_putchar('\n');
}
