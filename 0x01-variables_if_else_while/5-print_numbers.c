#include <stdio.h>

/**
 * main - numbers from 0 to 10
 * Description: prints out all the numbers from 0-10
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
	printf("%d", x);
	printf("\n");
	return (0);
}
