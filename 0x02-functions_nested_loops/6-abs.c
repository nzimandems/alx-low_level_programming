#include "main.h"

/**
 * _abs - prints the absolute value of an intager
 * @a: int to operate on
 *
 * return: success
 */
int _abs(int a)
{
	int n;
	int neg = -1;

	n = a * neg;
	if (a < 0)
	{
	return (n);
	}
	return (a);
}
